// Clerk webhook handler placeholder
export const clerkWebhookHandler = () => {
  // Implementation will be added later
  return async (req: any, res: any) => {
    return res.status(200).json({ message: 'Webhook handler placeholder' });
  };
}; 